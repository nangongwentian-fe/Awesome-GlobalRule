# AGENTS.md

## 1. 文件定位

1. 本文件是项目根级 Agent 规则入口，只保留仓库级强约束、经验沉淀路由规则、以及详细规则文档的索引。
2. 本文件不承载页面级、组件级、链路级细节；这些内容应写入更接近实际生效位置的局部 `AGENTS.md`，或写入 `docs/agent-rules/` 下的详细文档。
3. `AGENTS.md` 与 `CLAUDE.md` 职责等价；若当前工具使用 `CLAUDE.md`，应复用同样的规则结构。
4. 根级规则应保持稳定、精简、长期有效；详细内容按需读取，不应一股脑全部塞进根文件。

---

## 2. 总体原则

1. 默认使用中文回复；仅当用户明确要求其他语言时，才切换语言。
2. 用户明确要求优先于本文件默认规则。
3. 除非用户明确要求，否则优先遵循当前项目已有的技术栈、代码风格、目录结构、命名方式与工程约定。
4. Agent 应尽量像熟悉该项目已久的开发者一样工作：先理解已有实现，再做最小而正确的改动。
5. 对现有逻辑的理解应优先来自代码、配置、测试、文档和运行结果，而不是来自猜测。
6. 不要为了“看起来更好”而引入与项目现有风格明显不一致的新组织方式。

如需更详细的编码偏好与通用规范，按需读取：
- `docs/agent-rules/coding-style.md`
- `docs/agent-rules/code-organization.md`
- `docs/agent-rules/imports-and-paths.md`

---

## 3. 渐进式披露原则

1. 根级 `AGENTS.md` 只保留“始终应该知道”的最小必要规则。
2. 更详细、更具体、更局部的规则，应放在单独 Markdown 文件中，只有在任务相关时才读取。
3. 能在更近目录表达清楚的规则，不要上提到根级文件。
4. 能在详细文档表达清楚的规则，不要继续扩写根级文件。
5. 当某个模块开始积累稳定经验时，应优先新增该模块目录下的局部 `AGENTS.md`，而不是继续向根级文件追加细节。

详细索引见：
- `docs/agent-rules/README.md`

---

## 4. 作用域与优先级

1. 开始任务前，应先查看当前工作目录及其上层目录中是否存在 `AGENTS.md`。
2. 离当前文件、当前模块、当前目录越近的 `AGENTS.md`，优先级越高。
3. 高层级规则保持通用；低层级规则负责补充局部约束、局部例外和具体经验。
4. 当高层级规则与低层级规则冲突时，应优先遵循更接近当前工作的低层级规则。
5. 在没有更局部经验文件时，才回退使用更高层级经验文件。
6. 根级 `AGENTS.md` 不应承担所有模块的细节指导职责。

如需更详细的作用域设计与局部文件策略，按需读取：
- `docs/agent-rules/memory-scoping.md`
- `docs/agent-rules/local-agents-template.md`

---

## 5. 优先模仿项目中已有的优秀实现

1. 改代码前，应先阅读与当前任务最相近的现有实现，包括相邻文件、同目录文件、同类组件、同类页面、同类 hooks、同类 service 或同类工具函数。
2. 当功能与已有实现高度相似时，应优先参考并沿用已有设计，而不是自行发明新模式。
3. 模仿的是“已被项目验证的优秀实践”，不是机械复制；在复用模式时，应结合当前场景做必要简化。
4. 若现有项目中存在多种实现方式，应优先选择当前目录、当前子系统内已经普遍使用的方式。
5. 若发现现有实现存在明显设计缺陷，可以提出优化方案，但默认应先保证兼容和一致性，而不是立即推倒重来。

如需更详细的设计与组织规范，按需读取：
- `docs/agent-rules/component-design.md`
- `docs/agent-rules/code-organization.md`
- `docs/agent-rules/imports-and-paths.md`

---

## 6. Code Review 与输出要求

1. Code review 不应只关注语法、格式或命名，还应重点关注逻辑设计、组件封装、复用边界、状态管理和复杂度控制。
2. 指出问题时，应说明：
   - 当前设计为什么不合理
   - 会带来什么维护成本或风险
   - 更好的替代方式是什么
   - 不同方案之间的权衡是什么
3. 输出应尽量可直接落地。
4. 除非用户明确要求，否则不要擅自引入大规模重构、目录迁移、框架替换或风格统一类改动。
5. 在不确定时，优先保持最小修改范围，并保证对现有系统影响可控。

如需更详细说明，按需读取：
- `docs/agent-rules/code-review.md`

---

## 7. subagent 使用原则

1. 在执行环境支持的前提下，优先使用 subagent 处理可拆分、可并行、可独立验证的任务。
2. 主代理负责任务拆解、方案规划、结果汇总与最终验收。
3. 如果当前环境不支持 subagent，则按单代理方式执行，但仍应保持“先理解、再实施、再验收”的思路。
4. 使用 subagent 的目标是降低上下文压力、提高任务吞吐和结果稳定性，而不是为了形式上“必须多代理”。

如需更详细的拆分策略，按需读取：
- `docs/agent-rules/subagent-strategy.md`

---

## 8. 经验沉淀总原则

1. Agent 应主动寻找和复用已有经验，减少重复探索。
2. Agent 也应在合适时机主动沉淀新的有效经验，但经验沉淀的目标是提升后续任务质量，而不是机械堆积规则。
3. 当前代码、配置、测试、文档和实际运行行为是事实源头；经验/记忆只是辅助理解与复用的压缩总结。
4. 经验只有在未来大概率还会复用时才值得沉淀；一次性问题、偶然性偏好、临时上下文不应写入经验。
5. 经验系统必须允许修改、删除和下沉；它不是只增不减的历史垃圾堆。

如需更详细规则，按需读取：
- `docs/agent-rules/memory-system.md`
- `docs/agent-rules/memory-update-and-pruning.md`

---

## 9. 经验写入强制路由

1. 经验沉淀时，必须先判断作用域：局部 / 模块 / 项目 / 全局。
2. 默认采用“写低不写高”原则：能写在更近目录，就不要写到更高层级。
3. 仅当一条经验对当前仓库的大多数目录都成立时，才允许写入项目根级 `AGENTS.md`。
4. 只对单个页面、单个组件、单个目录、单条业务链路生效的经验，禁止写入项目根级 `AGENTS.md`，必须写入最接近该逻辑的目录。
5. 如果当前逻辑目录下还没有 `AGENTS.md`，且经验明显只作用于该目录或该模块，则应先在该目录创建局部 `AGENTS.md`，再写入经验。
6. 禁止使用“这条经验是否与当前项目相关”这种二分类问题来决定写入位置。
7. 如果作用域不明确，应优先问：
   - 这条经验只在当前目录/当前模块生效，对吗？
   - 这条规则是当前子系统通用，还是整个项目都通用？
8. 若用户未明确确认作用域，则禁止把局部经验上提到更高层级经验文件。
9. 当根级经验文件开始出现具体页面名、组件名、目录名、路由名、链路名等明显局部细节时，应优先判定为“写入位置过高”，并下沉到更局部的经验文件。

详细规则见：
- `docs/agent-rules/memory-scoping.md`

---

## 10. 经验过期与修订

1. 处理某个模块时，应主动检查该模块及其上层作用域中的经验文件是否仍与当前实现一致。
2. 如果经验与当前代码逻辑、配置、接口、目录结构、测试结果、文档或实际行为不一致，应视为可能过期。
3. 判断经验是否过期时，应以当前实现为准，而不是优先维护旧经验表述。
4. 如果原经验仅部分过时，应修改原条目。
5. 如果原经验已完全失效，应删除原条目。
6. 如果原经验只在局部模块内仍然有意义，但不再适合作为高层级经验存在，应将其下沉到更合适的局部经验文件。
7. 新增经验前，应先检查是否已有同主题旧经验；如已有，应优先合并、替换或重写。

详细规则见：
- `docs/agent-rules/memory-update-and-pruning.md`

---

## 11. 根级与局部经验文件维护策略

1. 根级 `AGENTS.md` 主要维护：
   - 仓库级工作规则
   - 全局编码偏好
   - 跨模块共通约定
   - 经验沉淀机制本身
2. 模块级或局部 `AGENTS.md` 主要维护：
   - 当前业务域的业务逻辑
   - 当前目录特有的实现约定
   - 当前模块反复出现的 review 结论
   - 当前模块的历史坑点、稳定模式与注意事项
3. 当发现根级经验文件开始频繁出现以下情况时，应优先考虑下沉经验，而不是继续扩写根文件：
   - 某个目录名、页面名、组件名被反复提及
   - 某类经验只在一个模块里成立
   - 某部分规则需要大量背景才能解释清楚
   - 某模块的经验更新频率明显高于全局规则
4. 不要为了“未来可能会用到”而过早为每个目录都创建局部经验文件；只有当该目录已经出现稳定经验沉淀需求时，才创建局部 `AGENTS.md`。
5. Agent 应主动维护这种分层结构；当局部经验足够成熟时，应倾向于新增或更新局部经验文件，而不是持续向根文件追加细节。

---

## 12. 推荐读取顺序

遇到不同任务时，按以下顺序选择性读取详细文档：

1. 通用编码与风格问题  
   读取：
   - `docs/agent-rules/coding-style.md`
   - `docs/agent-rules/code-organization.md`
   - `docs/agent-rules/imports-and-paths.md`

2. 组件设计、状态管理、封装边界问题  
   读取：
   - `docs/agent-rules/component-design.md`

3. Code review、评审意见输出  
   读取：
   - `docs/agent-rules/code-review.md`

4. 经验沉淀、经验分类、作用域判断  
   读取：
   - `docs/agent-rules/memory-system.md`
   - `docs/agent-rules/memory-scoping.md`

5. 经验过期、修订、删除、下沉  
   读取：
   - `docs/agent-rules/memory-update-and-pruning.md`

6. subagent 任务拆分与并行  
   读取：
   - `docs/agent-rules/subagent-strategy.md`

7. 准备在局部目录新建 `AGENTS.md`  
   读取：
   - `docs/agent-rules/local-agents-template.md`

---

## 13. 冲突处理优先级

当多条规则发生冲突时，按以下优先级处理：

1. 用户明确要求
2. 离当前工作最近作用域下的局部 `AGENTS.md`
3. 当前项目已有代码、配置、测试、文档和实际运行行为
4. 项目根级 `AGENTS.md`
5. 详细规则文档中的说明
6. 个人实现习惯

---

## 14. 最终要求

1. Agent 应主动利用已有经验，也应主动沉淀新的有效经验。
2. Agent 不应让经验系统变成只增不减的历史垃圾堆；经验必须随代码演进而持续更新。
3. Agent 的目标不是展示“会很多最佳实践”，而是在当前项目里持续做出一致、稳定、可维护、贴近现有风格的高质量修改。
4. 当根级经验文件开始承载过多局部细节时，Agent 应主动选择将经验下沉到更合适的模块目录，而不是继续堆积在项目根级文件中。